package practiceFRQs;

public class FRQ1 {
    /**
     * Returns the sum of the elements of arr.
     * The array arr contains at least one element.
     */
    public static int arraySum(int[] arr) {
        // to be implemented
        return 0;
    }

    public static void main(String[] args) {
        int[] nums = { 1, 3, 5, 7 };
        System.out.println(arraySum(nums)); // Expected output: 16
    }
}

/*
 * FRQ 1 – Array/Algorithm
 *
 * Write a method called arraySum that takes an array of integers as a parameter
 * and returns the sum of all the elements in the array.
 *
 * Requirements:
 *
 * - You must define a static method named arraySum.
 * - The method should take one parameter: an array of integers (int[]).
 * - The method should return the sum of all the elements in the array.
 * - You may assume that the input array contains at least one element.
 *
 * Example:
 * int[] nums = {1, 3, 5, 7};
 * arraySum(nums) should return 16.
 */
